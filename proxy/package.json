{
    "name": "soshiki-proxy",
    "version": "0.0.1",
    "scripts": {
        "start": "PORT=$PROXY_PORT node proxy.js",
        "start:dev": "PORT=$PROXY_PORT_DEV node proxy.js"
    },
    "dependencies": {
        "cors-anywhere": "^0.4.4"
    }
}
